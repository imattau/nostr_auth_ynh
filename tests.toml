#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    # Standard install, upgrade, backup/restore, and change-url tests are
    # derived automatically from the manifest (single install.domain
    # argument, no other install-time questions).
