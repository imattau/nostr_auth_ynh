Sign in to your existing YunoHost account with a linked Nostr identity
(NIP-07 browser extension to start), as an addition to password login - not
a replacement.

Link a Nostr public key to your account from an already-authenticated
session, then use `/nostr-login` afterwards to sign in with a single NIP-07
signature instead of a password.
