This add-on does not replace password login and does not modify SSOwat or
core YunoHost files. It links Nostr public keys to existing YunoHost
accounts and exposes a separate `/nostr-login` page.

You will still need a NIP-07 browser extension (e.g. Alby, nos2x) to link
and use a Nostr identity.
